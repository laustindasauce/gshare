package v1routes

// func ServerPrivateRoutes(a *fiber.App) {
// 	// Create routes group.
// 	route := a.Group("/api/v1")

// 	server := route.Group("/server", middleware.JWTProtected())

// 	server.Get("/cache/clear", controllers.ClearCache)
// }
